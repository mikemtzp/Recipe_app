<%= form_with model: recipe_food, url: user_recipe_recipe_foods_path, method: :post do |form| %>
  <% if recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_food.errors.count, "error") %> prohibited this recipe_food from being saved:</h2>

      <ul>
        <% recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :recipes_id, style: "display: block" %>
    <%= form.text_field :recipes_id %>
  </div>

  <div>
    <%= form.label :foods_id, style: "display: block" %>
    <%= form.text_field :foods_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
